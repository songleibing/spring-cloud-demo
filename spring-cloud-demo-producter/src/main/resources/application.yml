server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: spring-cloud-demo-producter:${server.port}
    prefer-ip-address: true    # 访问路径显示IP地址
spring:
  application:
    name: spring-cloud-demo-producter